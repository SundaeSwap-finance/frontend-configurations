{
  "name": "@sundaeswap-toolkit/theme",
  "version": "0.0.1-alpha.1",
  "module": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "yarn run build"
  },
  "dependencies": {
    "react": "^17.0.2",
    "styled-components": "^5.3.3"
  },
  "devDependencies": {
    "@types/ramda": "^0.27.60",
    "@types/styled-components": "^5.1.18",
    "ramda": "^0.27.1",
    "typescript": "~4.4.0"
  }
}
