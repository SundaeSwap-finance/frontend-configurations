{
  "name": "@sundaeswap-toolkit/uikit",
  "version": "0.0.1",
  "module": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "jest --verbose --runInBand"
  },
  "dependencies": {},
  "devDependencies": {
    "@testing-library/react-hooks": "^7.0.2",
    "@types/jest": "^27.0.3",
    "date-fns": "^2.27.0",
    "i18n": "^0.13.3",
    "i18next": "^21.6.0",
    "jest": "^27.4.5",
    "react-i18next": "^11.15.1",
    "ts-jest": "^27.1.1",
    "typescript": "~4.4.0"
  }
}
